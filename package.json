{
  "name": "claude-code-plugin-rest-api-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @tigz/claude-code-plugin-rest-api dev",
    "dev:example": "pnpm --filter basic-server dev",
    "test": "pnpm -r test",
    "test:e2e": "pnpm --filter basic-server test:e2e",
    "lint": "pnpm -r lint",
    "publish:lib": "pnpm --filter @tigz/claude-code-plugin-rest-api publish"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
